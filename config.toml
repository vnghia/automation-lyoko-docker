services = [
  "traefik",
  "postgres",
  "sftpgo",
  "sabredav",
  "ip-allowlist",
  "jaegertracing",
]
suffixes = ["toml", "yml", "sh", "json", "conf", "php", "py", "env"]
filenames = ["Dockerfile"]
output_dir = "rendered"
